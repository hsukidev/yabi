// Shared visual contract for roster cards (MuleCharacterCard + AddCard).
// Both must render with identical aspect-ratio and min-height so a row that
// mixes them stays flush.
export const ROSTER_CARD_MIN_HEIGHT = '120px';
export const ROSTER_CARD_ASPECT = '3/4';
