---
bump: minor
category: ui
---

Reintroduce the Mule Actions Menu kebab on roster surfaces: always visible top-right on every Character Card and as the trailing column on every List View row (hidden in Bulk Select Mode). It flips the Active Flag, sets or clears the daily/weekly/BM Clear Marks (rows shown per cadence eligibility), and deletes the mule instantly with an undo toast — without opening the drawer or starting a drag.
