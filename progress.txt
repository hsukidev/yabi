## Codebase Patterns
- Project uses Vitest for testing (configured in vite.config.ts with jsdom environment)
- Run tests with `npm run test`, typecheck with `npx tsc -b`, build with `npm run build`
- Test files live in `__tests__` directories next to the source they test
- Utility functions are in `src/utils/` and `src/data/`

## [2026-04-14] - US-001
- Set up Vitest with jsdom environment and globals config
- Added `npm run test` script to package.json
- Created formatMeso tests covering all acceptance criteria (7 tests, all passing)
- Files changed: package.json, vite.config.ts, src/utils/__tests__/meso.test.ts
- **Learnings for future iterations:**
  - Vitest config goes in vite.config.ts using `/// <reference types="vitest/config" />`
  - formatMeso takes (value, abbreviated) where abbreviated defaults to true
  - Build passes but warns about chunk size (>500kb) - can ignore for now
---

## [2026-04-14] - US-002
- Added tests for calculatePotentialIncome in src/data/__tests__/bosses.test.ts
- Tests cover: empty array, single boss, multiple bosses, unknown boss ID
- All 11 tests passing (7 formatMeso + 4 calculatePotentialIncome)
- **Learnings for future iterations:**
  - calculatePotentialIncome uses getBossById internally, unknown IDs return 0 value
---

## [2026-04-14] - US-003
- Extracted selectBoss pure function from BossChecklist.tsx handleSelect logic
- Created src/utils/selectBoss.ts with the extracted function
- Created src/utils/__tests__/selectBoss.test.ts with 5 tests covering all acceptance criteria
- Updated BossChecklist.tsx to use selectBoss instead of inline logic
- Changed replacement logic to use map() instead of filter+concat to preserve array order (matching PRD expectation)
- **Learnings for future iterations:**
  - selectBoss uses map() for in-family replacement to preserve original array order
  - BossChecklist.tsx was refactored to use selectBoss - future BossCheckboxList should also use it
---

## [2026-04-14] - US-004
- Created placeholder.png (200x300 PNG with character silhouette) in src/assets/
- Generated using sharp via SVG-to-PNG conversion
- **Learnings for future iterations:**
  - Placeholder image is at src/assets/placeholder.png, 200x300px with dark background and grey silhouette
---